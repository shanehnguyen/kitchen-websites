---
/* HomeContent — the full homepage body (hero video → letter → receipts →
   offering → guarantee/session → proof → FAQ → final band), extracted so it
   can render both on / (inside BaseLayout) and at the bottom of /scorecard
   (after the owner-only tease). Styles + hero-video script travel with it. */
import ReceiptStrip from './ReceiptStrip.astro';
import ClientShowcase from './ClientShowcase.astro';
import FAQ from './FAQ.astro';
import LetterBlock from './LetterBlock.astro';
import CTAButton from './CTAButton.astro';
import StickyMobileCTA from './StickyMobileCTA.astro';
import { homeFaqs } from '../data/faqs';
import { site } from '../data/site.config';

// hero=false drops the full-screen background-video hero (used on /scorecard,
// where the page opens on the featured video instead).
interface Props { hero?: boolean }
const { hero = true } = Astro.props;

const minutes = site.callLengthMinutes;

const letterStats = [
  { value: '2.7×', caption: 'more likely to be trusted with a complete Google profile' },
  { value: '270%', caption: 'more likely to get picked once you have your first five reviews' },
];

const closingStats = [
  { value: '97%', caption: 'read reviews before they pick a local business' },
  { value: '88%', caption: 'of mobile local searches end in a call or visit that day' },
  { value: '45%', caption: 'now use AI to find local pros, up from 6% a year ago' },
  { value: '5–9%', caption: 'more revenue from a single extra star' },
];
---
{hero && (
  <!-- 1. HERO (type-led, one CTA → /book) -->
  <section class="section home-hero">
    <div class="home-hero__bg" aria-hidden="true">
      <video class="home-hero__video" muted loop playsinline preload="auto" data-src="/video/hero-background.mp4"></video>
      <div class="home-hero__overlay"></div>
    </div>
    <div class="container home-hero__inner">
      <div class="home-hero__topline">
        <p class="eyebrow home-hero__kicker">Marketing for Kitchen &amp; Bath</p>
        <span class="home-hero__trust">
          <span class="home-hero__stars" role="img" aria-label="Rated 5 out of 5 stars">★★★★★</span>
          <span class="home-hero__trust-text">15+ success stories</span>
        </span>
      </div>
      <h1 class="home-hero__h1">The work used to bring the work.</h1>
      <p class="lede home-hero__sub">
        Now you’re staring at the calendar wondering where the next job comes from. I get kitchen &amp; bath
        businesses found by the homeowners already searching, and chosen over the business down the road.
      </p>
    </div>
  </section>
)}

<!-- Big featured video. Overlaps the hero on the homepage; sits flush at the top when there's no hero. -->
<div class:list={['home-reel', { 'home-reel--flush': !hero }]}>
  <div class="container">
    <div class="reel-video">
      <video class="reel-video__media" playsinline preload="metadata" poster="/video/kitchenwebsites-video1-poster.jpg" data-src="/video/kitchenwebsites-video1.mp4"></video>
      <button type="button" class="reel-video__play" aria-label="Play video"><span aria-hidden="true">▶</span></button>
    </div>
    <div class="home-reel__cta">
      <CTAButton href="/book" label="Claim your free strategy session" event="ReelToBook" size="lg" />
    </div>
  </div>
</div>

<!-- 2. THE LETTER -->
<section class="section home-letter">
  <div class="container">
    <LetterBlock dateLabel="Updated July 9, 2026">
      <p class="letter__salutation">Dear kitchen &amp; bath owner,</p>
      <p>The calendar used to fill itself. Word of mouth, repeat customers, the work brought the work.</p>
      <p>Then it slowed. One morning you caught yourself wondering where the next job was coming from.</p>
      <p>So you tried to fix it. Angi sold you tire-kickers. Houzz locked you in for a year. An SEO company took $1,500 a month and mailed you charts.</p>
      <p class="letter__beat">None of it filled the calendar.</p>
      <p>But the demand never left. The NKBA counts 10 million kitchens remodeled in America every year. One in ten households.</p>
      <p>The work is out there. The only question is whether you’re the one she finds, and the one she picks.</p>
      <p>Right now you might be neither. She goes looking, and either you don’t come up, or she lands on a competitor with a clean site and fresh reviews sitting right next to you.</p>
      <p>She can’t see your work. She can only see what’s on the screen, and his looks safer. So she calls him.</p>
      <p>74% of homeowners only trust reviews from the last three months. Five-star reviews from three years ago read like a business that slowed down.</p>
      <p>This isn’t you losing your touch. The first impression moved online, and she decides whether to call before she ever talks to you.</p>
      <p class="letter__beat">That’s what I fix.</p>
      <p>I get you found by the homeowners already looking, and I make you the one she picks. I build the site that closes her, and keep it working every month.</p>
      <p class="letter__beat">Found first. Picked first.</p>
      <div class="letter__cta">
        <CTAButton
          href="/book"
          label="Schedule your free strategy session"
          derisk={`${minutes} minutes, no pitch-slap. You leave with your plan either way.`}
          event="LetterMidToBook"
          size="lg"
        />
        <div class="letter__cta-receipts"><ReceiptStrip variant="compact" items={letterStats} /></div>
      </div>
      <p class="letter__sig">— Shane Nguyen, Kitchen Websites</p>
    </LetterBlock>
  </div>
</section>

<!-- PROOF (moved above the receipts) -->
<section class="section home-proof">
  <div class="container">
    <h2 class="home-proof__h">Be the next success story.</h2>
  </div>
  <div class="home-proof__showcase"><ClientShowcase /></div>
  <div class="container">
    <p class="home-proof__teaser">
      <a href="/about">Why I only do kitchen &amp; bath →</a>
    </p>
  </div>
</section>

<!-- OUR OFFERING — one plan, plain text (above the guarantee) -->
<section class="section home-offering">
  <div class="container">
    <div class="offer-plan">
      <h2 class="offer-plan__name">Done For You</h2>
      <p class="offer-plan__price">$750/month</p>
      <div class="offer-plan__body">
        <p class="offer-plan__lead">Everything in one package:</p>
        <ul class="offer-plan__list">
          <li>A website built to convert. <s>$1,500</s> included.</li>
          <li>Your Google rebuilt and run every month.</li>
          <li>Local SEO so you rise above the business two towns over.</li>
          <li>A steady stream of new five-star reviews.</li>
          <li>Set up for AI search, a name ChatGPT gives her.</li>
        </ul>
        <p>All of it working for you every month. You own every piece.</p>
      </div>
      <div class="offer-plan__cta">
        <CTAButton href="/done-for-you" label="See how Done For You works" event="OfferingToDoneForYou" size="lg" />
        <p class="offer-plan__alt">Want a website on its own instead? <a href="/websites">Start here →</a></p>
      </div>
    </div>
  </div>
</section>

<!-- bridge line: sits between the offer and the guarantee -->
<div class="home-bridge">
  <p>And I back the whole plan with one promise.</p>
</div>

<!-- 4. THE GUARANTEE → SESSION -->
<section class="section home-promise">
  <div class="container container--narrow">
    <h2 class="home-promise__h">If your calls don’t go up in ninety days, we work for free.</h2>

    <div class="home-promise__body">
      <p class="home-promise__beat">Read that again.</p>
      <p>Day one, we write down your numbers. How many calls you get, how many homeowners find you. Then I get to work, and you watch it happen every week.</p>
      <p class="home-promise__beat home-promise__beat--accent">Ninety days in, more homeowners are calling you than the day we started. If they’re not, you stop paying and I keep working until that number moves.</p>
      <p>Still flat three months later? We shake hands and you keep all of it. The profile, the reviews, the work. Yours.</p>
      <p>Picture the calendar then. The gaps filled. The phone going off while you’re on a job, because they find you first now, not the guy three streets over.</p>
      <p>If it doesn’t happen, I haven’t earned a dollar. And I don’t stop until it does.</p>
    </div>

    <hr class="home-promise__rule" />

    <p class="home-promise__hinge">Here’s how it works.</p>

    <div class="home-promise__session">
      <p>In thirty minutes, you’ll have the exact plan to turn the homeowners checking you out into booked jobs.</p>
      <p>I’ll audit the three things a homeowner checks before she calls: your Google profile, your website, and whether you show up when she asks AI for a remodeler nearby. Then I’ll show you what’s costing you the call.</p>
      <p>You leave with the plan in writing. Run it yourself or hand it to me.</p>
      <p>This is for kitchen and bath businesses doing great work but losing online. If that’s you, it’s the best half hour you’ll spend on your business this month.</p>
    </div>

    <div class="home-promise__cta">
      <CTAButton href="/book" label={`Book your ${minutes}-minute strategy session`} event="HomePromiseToBook" size="lg" />
    </div>
  </div>
</section>

<!-- 8. FAQ -->
<section class="section home-faq">
  <div class="container container--narrow">
    <h2 class="home-faq__h">The questions a burned owner actually asks.</h2>
    <div class="home-faq__list"><FAQ items={homeFaqs} /></div>
  </div>
</section>

<!-- 9. FINAL CTA BAND — the 30-minute teardown -->
<section class="section section--dark home-final">
  <div class="container container--narrow home-final__inner">
    <h2 class="home-final__h">You’ll walk away with the exact plan to beat them.</h2>
    <p class="home-final__body">
      {minutes} minutes, and you leave knowing precisely what to fix, in what order, to start winning the jobs
      going to the guy down the road. Hire me to run it or run it yourself. Kitchen and bath only.
    </p>
    <CTAButton href="/book" label={`Book your ${minutes}-minute strategy session`} event="FinalToBook" size="lg" />
    <div class="home-final__receipts"><ReceiptStrip variant="compact" items={closingStats} dark={true} /></div>
  </div>
</section>

<StickyMobileCTA href="/book" label="Book a Call" event="HomeStickyToBook" />

<script>
  // Lazy-init the hero background video: set the source after first paint so it
  // never blocks render, and skip playback under reduced-motion.
  const vid = document.querySelector<HTMLVideoElement>('.home-hero__video');
  if (vid && !window.matchMedia('(prefers-reduced-motion: reduce)').matches) {
    const start = () => {
      const src = vid.getAttribute('data-src');
      if (src && !vid.getAttribute('src')) {
        vid.src = src;
        vid.play().catch(() => {});
      }
    };
    const w = window as any;
    if (typeof w.requestIdleCallback === 'function') w.requestIdleCallback(start, { timeout: 1800 });
    else window.addEventListener('load', start);
  }

  // Featured reel: click-to-play. Load the source on first click so it never
  // costs anyone a download until they ask for it, then hand off to native controls.
  const reel = document.querySelector<HTMLElement>('.reel-video');
  const reelVid = reel?.querySelector<HTMLVideoElement>('.reel-video__media');
  const reelBtn = reel?.querySelector<HTMLButtonElement>('.reel-video__play');
  if (reel && reelVid && reelBtn) {
    reelBtn.addEventListener('click', () => {
      const src = reelVid.getAttribute('data-src');
      if (src && !reelVid.getAttribute('src')) reelVid.src = src;
      reelVid.controls = true;
      reel.classList.add('is-playing');
      reelVid.play().catch(() => {});
    });
  }
</script>

<style>
  /* Hero: full-bleed video background → dark overlay → centered content */
  .home-hero { position: relative; isolation: isolate; overflow: hidden; min-height: 100vh; min-height: 100svh; display: flex; align-items: flex-start; text-align: center; padding-top: clamp(8rem, 18vh, 12rem); padding-bottom: var(--section-y); }
  .home-hero__bg { position: absolute; inset: 0; z-index: 0; background: var(--cabinet); }
  .home-hero__video { position: absolute; inset: 0; width: 100%; height: 100%; object-fit: cover; object-position: center; }
  .home-hero__overlay {
    position: absolute; inset: 0;
    background:
      radial-gradient(120% 90% at 50% 45%, rgba(18, 16, 12, 0.45) 0%, rgba(18, 16, 12, 0.72) 70%, rgba(18, 16, 12, 0.84) 100%),
      linear-gradient(to bottom, rgba(18, 16, 12, 0.55), rgba(18, 16, 12, 0.78));
  }
  .home-hero__inner { position: relative; z-index: 1; width: 100%; }
  .home-hero__kicker { font-size: var(--fs-caption); color: var(--accent-strong); }
  .home-hero__h1 { margin: var(--space-5) auto 0; max-width: 20ch; font-size: var(--fs-hero); color: var(--porcelain); text-shadow: 0 2px 28px rgba(0, 0, 0, 0.45); }
  .home-hero__sub { margin: var(--space-6) auto 0; max-width: 52ch; font-size: var(--fs-lede); color: var(--paper-70); text-shadow: 0 1px 18px rgba(0, 0, 0, 0.5); }
  .home-hero__topline { display: flex; flex-wrap: wrap; align-items: center; justify-content: center; gap: var(--space-2) var(--space-4); }
  .home-hero__trust { display: inline-flex; align-items: center; gap: var(--space-2); }
  .home-hero__stars { color: #E9B949; letter-spacing: 2px; font-size: 1rem; line-height: 1; }
  .home-hero__trust-text { font-family: var(--font-body); font-size: var(--fs-caption); font-weight: var(--fw-body-semi); letter-spacing: 0.02em; color: var(--porcelain); }
  @media (prefers-reduced-motion: reduce) { .home-hero__video { display: none; } }

  /* Big featured-video placeholder overlapping the hero into the section below */
  .home-reel { position: relative; z-index: 5; margin-top: clamp(-26rem, -30vh, -14rem); margin-bottom: var(--space-12); }
  /* no hero above it (scorecard): sit flush at the top instead of overlapping */
  .home-reel--flush { margin-top: var(--space-10); }
  .reel-video {
    position: relative; aspect-ratio: 16 / 9; width: 100%; max-width: 68rem; margin-inline: auto;
    background: var(--cabinet); border: var(--rule-thick) solid var(--cabinet); border-radius: var(--radius-md);
    box-shadow: 0 32px 60px -24px rgba(18, 16, 12, 0.55);
    display: flex; align-items: center; justify-content: center; overflow: hidden;
  }
  .reel-video__media { position: absolute; inset: 0; width: 100%; height: 100%; object-fit: cover; object-position: center; background: var(--cabinet); }
  .reel-video::after {
    content: "Video"; position: absolute; bottom: var(--space-3); right: var(--space-4);
    font-family: var(--font-mono); font-size: var(--fs-small); letter-spacing: var(--tracking-caps);
    text-transform: uppercase; color: var(--paper-50); pointer-events: none;
  }
  .reel-video.is-playing::after { display: none; }
  .reel-video.is-playing .reel-video__play { display: none; }
  .reel-video__play {
    position: relative; z-index: 1;
    width: 84px; height: 84px; border-radius: 50%;
    border: 2px solid var(--porcelain); background: color-mix(in srgb, var(--porcelain) 12%, transparent);
    color: var(--porcelain); font-size: 1.6rem; line-height: 1; padding-left: 5px;
    display: flex; align-items: center; justify-content: center;
    transition: transform var(--duration-fast) var(--ease-out), background var(--duration-fast) var(--ease-out);
  }
  .reel-video__play:hover { transform: scale(1.06); background: color-mix(in srgb, var(--porcelain) 22%, transparent); }
  /* pushed well below the video so people watch before they click */
  .home-reel__cta { margin-top: var(--space-24); display: flex; justify-content: center; }
  /* mobile: the overlap pulls the video up onto the hero text, so stack it
     cleanly below the hero instead, and tighten the oversized CTA gap */
  @media (max-width: 48em) {
    /* content-driven hero height: with min-height:100vh the featured video was
       pushed a full screen below the text. min-height:auto drops it directly
       under the subhead, in normal flow, before the next section. */
    .home-hero { min-height: auto; padding-bottom: var(--space-8); }
    .home-reel { margin-top: var(--space-6); }
    .home-reel__cta { margin-top: var(--space-10); }
  }

  .home-offering { text-align: center; }
  .home-offering__h { margin: var(--space-3) auto 0; max-width: 28ch; }
  .home-offering__grid { margin-top: var(--space-8); display: grid; grid-template-columns: 1fr; gap: var(--space-5); }
  @media (min-width: 48em) { .home-offering__grid { grid-template-columns: 1fr 1fr; gap: var(--space-6); } }
  .offer-card { background: var(--bg-raised); border: var(--rule-width) solid var(--line); border-top: var(--rule-thick) solid var(--accent); border-radius: var(--radius-sm); padding: var(--space-8); display: flex; flex-direction: column; gap: var(--space-3); }
  .offer-card__tag { font-family: var(--font-body); font-size: var(--fs-caption); text-transform: uppercase; letter-spacing: var(--tracking-caps); font-weight: var(--fw-body-semi); color: var(--accent-deep); }
  .offer-card__title { font-family: var(--font-display); font-weight: var(--fw-display); text-transform: uppercase; font-size: var(--fs-h3); letter-spacing: -0.01em; }
  .offer-card__body { font-family: var(--font-serif); font-size: var(--fs-bodyL); line-height: 1.6; color: var(--ink-70); }
  .offer-card :global(.cta-block) { margin-top: auto; padding-top: var(--space-4); align-items: center; }

  /* one-plan offer — plain text, no box, "Done For You · $750/month" as the top line */
  .offer-plan { max-width: 65ch; margin-inline: auto; }   /* same measure as the dear-kitchen letter */
  .offer-plan__name { font-size: var(--fs-h2); }           /* display + uppercase inherited from base h2 — matches the other section headings */
  .offer-plan__price { margin-top: var(--space-2); font-family: var(--font-display); font-weight: var(--fw-display); font-size: var(--fs-h4); color: var(--accent-deep); }
  .offer-plan__body { margin-top: var(--space-5); }
  .offer-plan__body p { font-family: var(--font-serif); font-size: var(--fs-bodyL); line-height: 1.7; color: var(--ink); }
  .offer-plan__body p + p { margin-top: var(--space-4); }
  .offer-plan__lead { font-weight: var(--fw-body-semi); }
  .offer-plan__list { list-style: none; padding: 0; margin: var(--space-4) 0 var(--space-5); display: grid; gap: var(--space-3); }
  .offer-plan__list li { position: relative; padding-left: var(--space-6); font-family: var(--font-serif); font-size: var(--fs-bodyL); line-height: 1.5; color: var(--ink); }
  .offer-plan__list li::before { content: ""; position: absolute; left: 0; top: 0.55em; width: 14px; height: 2px; background: var(--accent); }
  .offer-plan__list s { color: var(--ink-50); text-decoration-thickness: 1px; }
  .offer-plan__cta { margin-top: var(--space-6); display: flex; flex-direction: column; align-items: center; }
  .offer-plan__cta :global(.cta-block) { align-items: center; }
  .offer-plan__alt { margin-top: var(--space-4); font-size: var(--fs-body-sm); color: var(--ink-50); }
  .offer-plan__alt a { color: var(--ink); font-weight: var(--fw-body-semi); }

  /* bridge line between the offer and the guarantee */
  .home-bridge { max-width: var(--container-narrow); margin-inline: auto; padding: 0 var(--gutter); text-align: center; }
  .home-bridge p { max-width: 42ch; margin: var(--space-8) auto; font-family: var(--font-serif); font-size: var(--fs-bodyL); color: var(--ink-70); }

  .home-receipts__h { margin-top: var(--space-3); max-width: 26ch; }
  .home-receipts__reads { list-style: none; margin: var(--space-8) 0 0; padding: 0; display: grid; grid-template-columns: 1fr; gap: var(--space-4); }
  @media (min-width: 56em) { .home-receipts__reads { grid-template-columns: 1fr 1fr; gap: var(--space-5); } }
  .home-receipts__reads li {
    background: var(--bg-raised);
    border: var(--rule-width) solid var(--line);
    border-top: var(--rule-thick) solid var(--accent);
    border-radius: var(--radius-sm);
    padding: var(--space-5) var(--space-6);
    font-size: var(--fs-body); line-height: 1.5; color: var(--ink);
  }
  @media (min-width: 56em) { .home-receipts__reads li:last-child { grid-column: 1 / -1; } }
  .home-receipts__reads strong { color: var(--accent-deep); font-weight: var(--fw-body-bold); }

  .home-promise .eyebrow { text-align: center; }
  .home-promise__h { margin: var(--space-3) auto 0; max-width: 24ch; text-align: center; }
  .home-promise__body { margin: var(--space-8) auto 0; max-width: 65ch; }
  .home-promise__body p { font-family: var(--font-serif); font-size: var(--fs-bodyL); line-height: 1.7; color: var(--ink); margin-bottom: var(--space-5); }
  .home-promise__body p:last-child { margin-bottom: 0; }
  .home-promise__body strong { font-weight: var(--fw-body-bold); }
  .home-promise__body .home-promise__beat { font-family: var(--font-display); font-weight: var(--fw-display); font-size: var(--fs-h4); letter-spacing: -0.01em; line-height: 1.2; }
  .home-promise__body .home-promise__beat--accent { color: var(--accent-deep); }
  .home-promise__trust { margin: var(--space-8) auto 0; max-width: 56ch; padding-left: var(--space-5); border-left: var(--rule-thick) solid var(--accent); }
  .home-promise__trust p { font-family: var(--font-serif); font-size: var(--fs-bodyL); line-height: 1.65; color: var(--ink); margin-bottom: var(--space-4); }
  .home-promise__trust p:last-child { margin-bottom: 0; }
  .home-promise__rule { margin: var(--space-10) auto; border: 0; border-top: var(--rule-thick) solid var(--rule); width: 5rem; }
  .home-promise__hinge { text-align: center; margin: 0 auto; max-width: 32ch; font-family: var(--font-display); font-weight: var(--fw-display); text-transform: uppercase; font-size: var(--fs-h2); letter-spacing: var(--tracking-display); line-height: var(--lh-tight); color: var(--ink); }
  .home-promise__session { margin: var(--space-6) auto 0; max-width: 65ch; }
  .home-promise__session p { font-family: var(--font-serif); font-size: var(--fs-bodyL); line-height: 1.7; color: var(--ink-70); margin-bottom: var(--space-4); }
  .home-promise__session p:last-child { margin-bottom: 0; }
  .home-promise__cta { margin-top: var(--space-8); display: flex; justify-content: center; }

  .letter__cta-receipts { margin-top: var(--space-6); max-width: 22rem; }

  .home-proof__h { margin-top: var(--space-3); }
  .home-proof__sub { margin-top: var(--space-4); max-width: 56ch; }
  .home-proof__showcase { margin-top: var(--space-10); }
  .home-proof__teaser { margin-top: var(--space-8); color: var(--ink-70); }

  .home-faq__h { margin-top: 0; max-width: none; }
  .home-faq__list { margin-top: var(--space-8); }

  .home-final__inner { text-align: center; display: flex; flex-direction: column; align-items: center; gap: var(--space-6); }
  .home-final__h { max-width: 24ch; }
  .home-final__body { max-width: 56ch; color: var(--dark-soft); }
  .home-final__receipts { margin-top: var(--space-6); width: 100%; }
</style>
